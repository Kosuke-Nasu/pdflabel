# CLAUDE.md

このファイルは、このリポジトリで作業する Claude Code 向けのガイドです。

## プロジェクト概要

テキストを入力し、ラベル用紙に合わせた PDF を生成する Web アプリケーション。

## 技術仕様

- HTML + JavaScript のみで構成する。サーバー不要で、ブラウザだけで動作すること
- PDF 生成には jsPDF（JavaScript で PDF を作成するライブラリ）を CDN（ライブラリをネット経由で配信する仕組み）から読み込んで使う
  - バージョンは固定する: `https://cdnjs.cloudflare.com/ajax/libs/jspdf/4.2.1/jspdf.umd.min.js`
- 動作対象ブラウザ: 最新版の Chrome / Edge
- 起動方法: `index.html` をブラウザで直接開く
- インターネット接続を前提とする（jsPDF とフォントを CDN から取得するため）

### 日本語フォント

jsPDF の標準フォントは日本語に対応していないため、日本語フォントを PDF に埋め込む（フォントデータを PDF の中に含める）。

- 使用フォント: BIZ UDゴシック（読みやすさを重視したユニバーサルデザインフォント。OFL ライセンス（商用利用・再配布が可能なフォント用ライセンス）で無料）
- 取得元（リポジトリのコミットを固定した URL）:
  `https://cdn.jsdelivr.net/gh/google/fonts@01ac47e0b1cddbc298334c69a5dfafb0205ffa28/ofl/bizudgothic/BIZUDGothic-Regular.ttf`
- ページを開いた時点で `fetch` で読み込み、Base64（バイナリを文字列で表す形式）に変換して `addFileToVFS` と `addFont` で jsPDF に登録する
- フォントは約 4.5MB あるため、読み込み中は「フォント読み込み中…」と表示し、PDF 生成ボタンを押せないようにする
- 読み込みに失敗したら、エラーメッセージを表示する
- 画面のプレビュー表示にも、同じフォントを `FontFace` API（JavaScript からフォントを登録する仕組み）で適用する

## ラベルの仕様

### 用紙と入力項目

入力値の単位はすべて mm で、小数第1位まで入力できる（文字サイズのみ pt（ポイント。1pt ≒ 0.353mm））。

| 項目 | 初期値 |
|---|---|
| 用紙サイズ | A4（210mm × 297mm）固定 |
| 余白（上・下・左・右） | 各 10mm |
| ラベルの幅 × 高さ | 60mm × 30mm |
| ラベル間の余白（横方向・縦方向を別々に設定） | 横 5mm・縦 5mm |
| ラベル内側の余白 | 2mm |
| 文字サイズ（上限） | 12pt |
| 枠線を印字する | オフ |

### 並べ方

- 列数と行数は、入力値から自動で計算する
  - 列数 = floor((210 − 左余白 − 右余白 + 横間隔) ÷ (ラベル幅 + 横間隔))
  - 行数 = floor((297 − 上余白 − 下余白 + 縦間隔) ÷ (ラベル高さ + 縦間隔))
- 配置の起点は「左余白・上余白」の位置とする。中央寄せはしない（市販のラベル用紙は左上からの位置で寸法が決まっているため）
- 右余白・下余白は「ここまでしか使わない」という上限として扱う
- 計算した「列数 × 行数 = 合計枚数」を画面に表示する
- 出力は A4 1ページのみ

### テキストの配置

- 入力欄は `textarea`（複数行の入力欄）とし、改行をそのまま反映する
- 各ラベルの中で、水平方向・垂直方向ともに中央揃えにする
- 行の高さは文字サイズの 1.2 倍とする
- 印字できる範囲は、ラベルの内側余白を除いた部分とする
- 設定した文字サイズで収まらない場合は、収まるまで自動で文字を小さくする（下限 4pt）。下限でも収まらない場合は、警告を表示する
- 1行が長すぎて横にはみ出す場合も、自動で文字を小さくして対応する（勝手に折り返さない）

### 入力値のチェック

- マイナスの値、空欄、数値以外が入力された場合は、該当する欄の近くにエラーを表示する
- ラベルが1枚も収まらない場合は、「ラベルが用紙に収まりません」と表示する
- エラーがある間は、PDF 生成ボタンを押せないようにする
- テキストが空の場合も、PDF 生成ボタンを押せないようにする

## 画面と出力

- 画面の表示言語は日本語
- 画面の構成: 左に入力フォーム、右にプレビュー（完成イメージ）。画面の幅が狭いときは縦に並べる
- プレビューは入力のたびに自動で更新する。用紙全体を縮小表示し、ラベルの枠と文字を描く
- 「PDF をダウンロード」ボタンで `labels.pdf` を保存する
- 枠線オンのときは、ラベルの外周に細い灰色の線を印字する（位置合わせのテスト印刷用）
- ボタンの近くに次の注意書きを表示する:
  「印刷するときは、倍率を『実際のサイズ』または『100%』にしてください。『用紙に合わせる』を選ぶと位置がずれます」
- 入力内容（テキストと全設定）は localStorage（ブラウザにデータを保存する仕組み）に保存し、次に開いたときに復元する。「設定を初期値に戻す」ボタンも用意する

## コーディング規約

- コメントは日本語で書く
- `index.html` 1 ファイルに完結させる（外部 CSS・外部 JS ファイルは作らない）
- ビルド工程（コードを変換・まとめる前処理）は使わない
- 寸法の計算（何列・何行か、各ラベルの位置）は、画面表示や PDF 生成とは別の関数にまとめる。プレビューと PDF で同じ計算結果を使うため

## 作業ルール

- ファイルを変更する前に、必ず `git commit` で現在の状態を記録すること
- コミットメッセージは日本語で書く
- 初出の専門用語には、日本語で一言の説明を添えること
